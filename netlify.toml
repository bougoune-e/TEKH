[build]
  command = "npm run build"
  publish = "dist"

# Optional: set Node version for build image
[build.environment]
  NODE_VERSION = "18"

# SPA redirect so client-side routing works on refresh
[[redirects]]
  from = "/*"
  to = "/index.html"
  status = 200
