import EmptyState from "../components/EmptyState";

const Categories = () => {
  return (
    <EmptyState title="Aucune catégorie" description="Branche l'API pour gérer marques, modèles et états." />
  );
};

export default Categories;
